<p id="notice"><%= notice %></p>
<div class="card mx-auto mt-3" style="width: 30%">
  <div class="card-header  bg-secondary text-light">Seat Information
  </div>
  <div class="card-body">
    <div>
      <strong>Class name:</strong>
      <%= @seat_class.class_name %>
    </div>

    <div>
      <strong>Price: </strong>
      <%= @seat_class.price %>
    </div>

    <div>
      <strong>Food service:</strong>
      <%= @seat_class.food_service %>
    </div>

    <div>
      <strong>Seat type:</strong>
      <%= @seat_class.seat_type %>
    </div>

    <div>
      <strong>Leg room:</strong>
      <%= @seat_class.leg_room %>
    </div>
  </div>
<%= link_to 'Edit', edit_seat_class_path(@seat_class) %>
<%= link_to 'Back', seat_classes_path %>
</div>